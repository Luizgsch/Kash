/** Cookie lida no servidor para filtrar dashboard, histórico e contexto de gravação. */
export const ACTIVE_WALLET_COOKIE = "kash_active_wallet";

/** Valor da cookie quando o utilizador escolhe "Geral" (soma de todas as wallets). */
export const ACTIVE_WALLET_GERAL = "__GERAL__";
